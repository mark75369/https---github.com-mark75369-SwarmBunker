fastapi>=0.104
uvicorn[standard]>=0.24
python-multipart>=0.0.6
pyyaml>=6.0
markdown>=3.5
# Phase A.0F.patch-round2-P1 (CODEX 2nd review finding #1):
# httpx is required by fastapi.testclient.TestClient (used in
# tests/test_endpoints_smoke.py Stage B). Without it `from fastapi.testclient
# import TestClient` raises ImportError and Stage B silently skips, masking
# acceptance failures. host running `pip install -r requirements.txt` must
# pick this up so Stage B is actually executed, not skipped.
httpx>=0.27
