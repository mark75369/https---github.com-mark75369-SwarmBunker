狀態：DRAFT（骨架）  
版本：v0.1  
最後更新：2026-05-19  
適用範圍：交付給 Unity / 翻譯 / 配音的 workflow  
優先級：中（Phase D 完成後補真實案例）

# 情境 5：交付到下游

> ⏳ **本文為骨架**，待 Phase D 完成後補真實案例。

# 1. 適合情境

你寫完一整章 / 整個遊戲後，要把對話資料交給程式 / 翻譯 / 配音。

# 2. 工具的「交付」邊界

工具產 **JSON + MD 雙吐**。**轉檔到引擎、翻譯本身、錄音流程都在工具外**。

```
工具負責：產 export/<instance>_<date>.json + .md
        ↓
工具外：
- 程式：寫 5-10 分鐘 script，把 JSON 轉成引擎可吃格式（Unity ScriptableObject / Yarn Spinner / Ink / 等）
- 翻譯：把 JSON 餵入翻譯系統，產各語 .po / .json
- 配音：從 JSON 抽 KEY + content + speaker + emotion → 給聲優錄音表
```

---

# 3. Export 流程

## Step 1：開 Export Panel

前端 Dashboard 點「📤 開啟 Export panel」。

## Step 2：選範圍 / 格式 / 推送方式

```
範圍：⦿ 全部 / ○ 僅大綱 / ○ 僅本場景
格式：☑ JSON  ☑ MD  ☐ 含已刪除 KEY
推送方式：⦿ clipboard / ○ POST 到本地 LLM / ○ POST 到 Claude API（TBD）
```

## Step 3：點「複製 Prompt」

整段 prompt 自動複製到剪貼簿。

## Step 4：貼到 Claude Code

agent 收到後跑 export → 產 `export/<instance>_<date>.json` + `.md`。

完成回報含 records 數 / 各 record_type 分布 / 檔案 size。

## Step 5：JSON 給下游工種

把 `export/<instance>_<date>.json` 透過任何方式（git / email / share folder）給：

- **程式工程師**：寫 script 轉成遊戲引擎格式
- **翻譯協作方**：餵入翻譯軟體
- **聲優經紀**：產配音表

---

# 4. JSON 結構

```json
{
  "manifest": {
    "export_version": "1.0",
    "exported_at": "2026-05-19T14:30:00Z",
    "instance_id": "魔法學院",
    "spec_version": "data_format_spec_v0.1",
    "stats": {
      "total_entities": 47,
      "total_dialogue_lines": 1532,
      "total_art_assets": 23
    },
    "entity_type_registry": { ... },
    "qa_type_registry": { ... }
  },
  "records": [
    {
      "record_type": "dialogue_line",
      "key": "dlg.ch01.s03.l001",
      "speaker": "C-主角A",
      "content": "你為什麼藏這本書？",
      "portrait": "A-portrait-主角A-anger",
      "bgm": "A-bgm-tension-01",
      "sfx": [],
      "status": "active",
      "scene_id": "S-01-03"
    },
    ...
  ]
}
```

詳見 `_design/DATA_FORMAT_SPEC.md` §9 + Contract A.7。

---

# 5. 下游工種怎麼吃

## 5.1 程式工程師（Unity 為例）

```csharp
// 5 分鐘 script，把 JSON 轉成 ScriptableObject
[Serializable]
public class DialogueLine {
    public string key;
    public string speaker;
    public string content;
    public string portrait;
    public string bgm;
    public string scene_id;
}

// 讀 JSON
var json = File.ReadAllText("export/魔法學院_2026-05-19.json");
var data = JsonUtility.FromJson<ExportData>(json);

// 轉成 Unity 對話 asset
foreach (var record in data.records) {
    if (record.record_type == "dialogue_line") {
        // 產 ScriptableObject ...
    }
}
```

## 5.2 翻譯

直接把 JSON 餵入翻譯系統（POEditor / Crowdin / 自家翻譯軟體）：

- `key` 作為翻譯 ID
- `content` 作為原文
- `speaker` + `portrait` 作為譯者參考 context

翻譯回來後產各語 .po / .json 給程式。

## 5.3 配音

從 JSON 抽：

| 配音表欄位 | 從 JSON 取 |
|---|---|
| line ID | `key` |
| 說話角色 | `speaker` |
| 對白文字 | `content` |
| 立繪情緒（演技參考）| `portrait`（如 `A-portrait-主角A-anger` → anger）|
| 場景 context | `scene_id` |

---

# 6. 重要原則

1. **JSON 是 contract**：下游不應依賴 MD（MD 是給人讀的）
2. **不寫實檔路徑** — A-* 只存 KEY metadata，實檔對應由外部處理
3. **不存多語對白** — 翻譯外部 i18n 系統處理
4. **read-only export** — Export 不修改 source / 不改 phase_log

---

# 7. 待補內容（Phase D 完成後補）

- [ ] 完整 Export 截圖
- [ ] Unity ScriptableObject 完整 script 範例
- [ ] Yarn Spinner / Ink 轉換 script 範例
- [ ] 翻譯軟體（POEditor / Crowdin / Smartcat）對接流程
- [ ] 配音表範例（Excel / Google Sheets）
- [ ] 增量 export（只 export 修改過的部分）
- [ ] 推送方式 lifecycle（Phase B+ POST 本地 LLM）
