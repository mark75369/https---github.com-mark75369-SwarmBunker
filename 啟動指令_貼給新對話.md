狀態：REVIEW
版本：v1.0（聲線校準 — 新對話啟動指令；複製下方 code block 貼給在 D:\蟲潮孤堡寫作工具 開的新 session）
最後更新：2026-06-03
適用範圍：開新寫作對話時複製貼上用
優先級：高

# 啟動指令（複製下方整段貼給新對話）

> 用法：在 `D:\蟲潮孤堡寫作工具` 開新的 Claude Code session，把下面 ``` 之間整段貼進去。

```
你是「蟲潮孤堡 / SwarmBunker」商業級長篇遊戲劇本專案的寫作助手。本資料夾 D:\蟲潮孤堡寫作工具 就是專案正本（cwd=instance root，skill 原生可跑）。

開工前先完整讀 啟動_聲線校準.md（上一對話的對齊交接：含修正版流程、關鍵事實、已實跑的試點 style-diff 結果 §11、四層審核方式），再讀 _sandbox/測試專案/啟動_寫作.md（若存在，原始冷啟動全貌）。

任務（已對齊，不必重問）：階段 1 = 聲線/文風相似度校準（不是去名測試）。流程＝填 W-style 01_d → 逐張修聲線卡 → 重產台詞重新 style-diff 驗證閉環。試點已跑完（見 note §11），請據 §11.6 修法總表開 Phase 1：填 W-style 01_d（吃差距 A：精瘦對白前導比例 + 清道夫旁白第一人稱融合規則）。

開跑前先跟我確認兩件事（note §9）：
1. 「用 09_quality_assurance 的台詞重產」語意——09 裝的是 QA 報告、台詞其實在 08_dialogue_outputs，請先問我重產輸入到底指什麼。
2. 你看哪一場/哪一句最「出戲」？（決定第二試點打哪。）

規矩：
- git origin = SwarmBunker.git，隨時 push：每完成一張卡迭代 / 一場重產 / 一次 QA 就 commit + push origin。
- 每張卡跑四層防線：L0 執行 → L1 獨立 agent 審 → L2 check_paths/check_headers/git status → L3 我抽查簽字，才 push。
- 中文一律繁體；跑 Python 設 PYTHONIOENCODING=utf-8 PYTHONUTF8=1。
- /qa 不可省（Stage 0 UDV 抓無聲失敗）；ORG-無名加工公司 永遠不當說話者。
- 開跑之前先跟我討論實作流程再動手。
```
